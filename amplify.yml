version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install npm
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/src*'
  cache:
    paths:
      - node_modules/**/*
